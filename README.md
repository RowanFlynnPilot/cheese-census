# The Cheese Census

A census of Wisconsin cheese, from the Wausau Pilot & Review — a statewide cheese
database and reader tool. Scraped from public sources, merged and validated into
static JSON, served by a React frontend.

- `SCHEMA.md` — the canonical data model and pipeline rules
- `CLAUDE.md` — working context, conventions, and current status
